# Finde jüngsten Run-Ordner OHNE run_dir zu evaluieren (vermeidet Rekursion).
# Findet nur Verzeichnisse mit metadata.yml (robust für alle Run-Formate)
# Sortiert nach mtime (Modifikationszeit) - funktioniert auf macOS und Linux
- name: Determine latest run directory under results/
  ansible.builtin.set_fact:
    rd: "{{ workdir }}/results/{{ lookup('pipe', 'python3 -c \"import os, glob; dirs = [d for d in glob.glob(\\\"' ~ workdir ~ '/results/*\\\") if os.path.isdir(d) and os.path.exists(os.path.join(d, \\\"metadata.yml\\\"))]; print(max(dirs, key=os.path.getmtime).split(\\\"/\\\")[-1] if dirs else \\\"\\\")\"') | trim }}"

- name: Ensure results dir exists
  ansible.builtin.file:
    path: "{{ rd }}"
    state: directory

- name: Dump docker logs (Parallel)
  ansible.builtin.shell: |
    mkdir -p "{{ rd }}"
    # Nodes logs
    for i in $(seq -f "%02g" 1 {{ node_count }}); do
      docker logs "node$i" > "{{ rd }}/node$i.log" 2>&1 &
    done
    # txgen logs
    {% for item in txgen_shards %}
    docker logs "txgen_{{ item.id }}" > "{{ rd }}/txgen_{{ item.id }}.log" 2>&1 &
    {% endfor %}
    # block scheduler logs
    docker logs block_scheduler > "{{ rd }}/block_scheduler.log" 2>&1 &
    # injection gateway logs
    docker logs injection_gateway > "{{ rd }}/injection_gateway.log" 2>&1 &
    wait
  changed_when: true

- name: Snapshot network and mempool status
  ansible.builtin.shell: |
    docker exec node01 bitcoin-cli -regtest getchaintips > "{{ rd }}/chaintips.json"
    docker exec node01 bitcoin-cli -regtest getmempoolinfo > "{{ rd }}/mempool.json"
    docker exec node01 bitcoin-cli -regtest getpeerinfo > "{{ rd }}/peerinfo.json"
    docker exec node01 bitcoin-cli -regtest listbanned > "{{ rd }}/banned.json"
    docker exec node01 bitcoin-cli -regtest getnettotals > "{{ rd }}/nettotals.json"
  changed_when: true

- name: Copy txgen shard artefacts from volumes (Parallel)
  ansible.builtin.shell: |
    {% for item in txgen_shards %}
    (
      volume="{{ network_name }}_txlogs_{{ item.log_suffix }}"
      if docker volume inspect "${volume}" >/dev/null 2>&1; then
        docker run --rm -v "${volume}":/v -v "{{ rd }}":/out alpine:3.19 sh -lc '
          set -e
          suffix="{{ item.log_suffix }}"
          [ -f "/v/txlog_${suffix}.csv" ] && cp -f "/v/txlog_${suffix}.csv" "/out/txgen_{{ item.id }}_txlog.csv" || ([ -f "/v/txlog.csv" ] && cp -f "/v/txlog.csv" "/out/txgen_{{ item.id }}_txlog.csv" || true)
          [ -f "/v/txlog_${suffix}_performance.csv" ] && cp -f "/v/txlog_${suffix}_performance.csv" "/out/txgen_{{ item.id }}_txlog_performance.csv" || ([ -f "/v/txlog_performance.csv" ] && cp -f "/v/txlog_performance.csv" "/out/txgen_{{ item.id }}_txlog_performance.csv" || true)
          [ -f "/v/txlog_${suffix}_errors.csv" ] && cp -f "/v/txlog_${suffix}_errors.csv" "/out/txgen_{{ item.id }}_txlog_errors.csv" || ([ -f "/v/txlog_errors.csv" ] && cp -f "/v/txlog_errors.csv" "/out/txgen_{{ item.id }}_txlog_errors.csv" || true)
        '
      fi
    ) &
    {% endfor %}
    wait
  changed_when: false

- name: Copy funding state artefacts
  ansible.builtin.copy:
    src: "{{ workdir }}/state/{{ item.src }}"
    dest: "{{ rd }}/{{ item.dest }}"
    remote_src: true
  loop:
    - { src: "funding_snapshot.json", dest: "funding_snapshot.json" }
    - { src: "mining_targets.json", dest: "mining_targets.json" }
  ignore_errors: true

- name: Copy mempool timeseries data
  ansible.builtin.copy:
    src: "{{ workdir }}/state/mempool_timeseries.csv"
    dest: "{{ rd }}/mempool_timeseries.csv"
    remote_src: true
  ignore_errors: true

- name: Erzeuge mining.csv aus Scheduler-Log
  ansible.builtin.shell: |
    python3 - <<'PY'
    import csv
    from pathlib import Path

    log_path = Path("{{ rd }}/block_scheduler.log")
    if not log_path.exists():
        raise SystemExit(0)

    events = []
    with log_path.open("r", encoding="utf-8") as handle:
        for line in handle:
            if not line.startswith("BLOCK_EVENT,"):
                continue
            parts = line.strip().split(",", 4)
            if len(parts) != 5:
                continue
            _, ts, number, miner, block_hash = parts
            try:
                number_int = int(number)
            except ValueError:
                continue
            events.append({
                "timestamp_utc": ts,
                "block_number": number_int,
                "miner": miner,
                "block_hash": block_hash,
            })

    if not events:
        raise SystemExit(0)

    events.sort(key=lambda e: e["block_number"])
    out_path = Path("{{ rd }}/mining.csv")
    with out_path.open("w", encoding="utf-8", newline="") as out:
        writer = csv.DictWriter(out, fieldnames=["timestamp_utc", "block_number", "miner", "block_hash"])
        writer.writeheader()
        writer.writerows(events)
    PY
  changed_when: true

- name: Merge shard CSV artefacts into aggregated files
  ansible.builtin.shell: |
    python3 - <<'PY'
    import csv
    import glob
    import os

    run_dir = "{{ rd }}"
    patterns = {
        "txlog.csv": "txgen_*_txlog.csv",
        "txlog_performance.csv": "txgen_*_txlog_performance.csv",
        "txlog_errors.csv": "txgen_*_txlog_errors.csv",
    }

    for target, pattern in patterns.items():
        files = sorted(glob.glob(os.path.join(run_dir, pattern)))
        if not files:
            continue

        rows = []
        fieldnames = None
        for path in files:
            with open(path, "r", encoding="utf-8", newline="") as handle:
                reader = csv.DictReader(handle)
                if fieldnames is None:
                    fieldnames = reader.fieldnames
                for row in reader:
                    rows.append(row)

        if not fieldnames:
            continue

        key = None
        for candidate in ("submit_ts_utc", "timestamp_utc"):
            if candidate in fieldnames:
                key = candidate
                break
        if key:
            rows.sort(key=lambda r: r.get(key, ""))

        out_path = os.path.join(run_dir, target)
        with open(out_path, "w", encoding="utf-8", newline="") as out:
            writer = csv.DictWriter(out, fieldnames=fieldnames)
            writer.writeheader()
            writer.writerows(rows)
        
        # Cleanup: Remove redundant shard files after successful merge
        for path in files:
            try:
                os.remove(path)
            except OSError:
                pass
    PY
  changed_when: true

- name: Extract transaction confirmations (if txlog exists)
  ansible.builtin.shell: |
    if [ -f "{{ rd }}/txlog.csv" ]; then
      python3 analysis/extract_confirmations.py --run-dir "{{ rd }}" --node node01
    else
      echo "No txlog.csv found, skipping confirmation extraction"
      echo "txid,submit_time,confirm_time,confirmations,block_height" > "{{ rd }}/confirmations.csv"
    fi
  args:
    chdir: "{{ workdir }}"
  changed_when: true

- name: Collect node health status (Parallel)
  ansible.builtin.shell: |
    echo "node,status,block_height,peer_count,banned_count" > "{{ rd }}/node_health.csv"
    # Create temp files for each node to avoid race conditions when writing to the CSV
    TMP_DIR=$(mktemp -d)
    for i in $(seq -f "%02g" 1 {{ node_count }}); do
      (
        node="node$i"
        if docker ps --format "{{"{{"}} .Names {{"}}"}}" | grep -q "^$node$"; then
          height=$(docker exec $node bitcoin-cli -regtest getblockcount 2>/dev/null || echo "N/A")
          peers=$(docker exec $node bitcoin-cli -regtest getconnectioncount 2>/dev/null || echo "N/A")
          banned=$(docker exec $node bitcoin-cli -regtest listbanned 2>/dev/null | jq '. | length' 2>/dev/null || echo "0")
          echo "$node,running,$height,$peers,$banned" > "$TMP_DIR/$node"
        else
          echo "$node,stopped,N/A,N/A,N/A" > "$TMP_DIR/$node"
        fi
      ) &
    done
    wait
    # Aggregate temp files
    cat $TMP_DIR/node* >> "{{ rd }}/node_health.csv"
    rm -rf "$TMP_DIR"
  changed_when: true

- name: Save final mining statistics summary
  ansible.builtin.shell: |
    if [ -f "{{ rd }}/mining.csv" ]; then
      python3 << 'PYTHON_SCRIPT'
    import csv
    import json
    from collections import Counter
    
    with open('{{ rd }}/mining.csv', 'r') as f:
        reader = csv.DictReader(f)
        miners = [row['miner'] for row in reader if row['miner']]
    
    stats = Counter(miners)
    total = sum(stats.values())
    
    if total > 0:
        with open('{{ rd }}/mining_summary.json', 'w') as f:
            summary = {
                'total_blocks': total,
                'miners': {miner: {'blocks': count, 'percentage': round(count/total*100, 2)} 
                           for miner, count in stats.items()},
                'unique_miners': len(stats)
            }
            json.dump(summary, f, indent=2)
    PYTHON_SCRIPT
    fi
  changed_when: true
