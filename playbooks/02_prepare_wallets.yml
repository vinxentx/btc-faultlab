- hosts: controller
  gather_facts: false
  vars:
    funding_host_port: "127.0.0.1:{{ wallet_rpc_base }}"
  tasks:
    - name: Determine whether to skip funding setup
      ansible.builtin.set_fact:
        skip_funding_setup: "{{ use_chain_snapshot | default(false) | bool and snapshot_restore_wallets | default(true) | bool and snapshot_restore_state | default(true) | bool }}"

    - name: Clean up old txgen signal files
      ansible.builtin.shell: |
        rm -f {{ workdir }}/state/txgen_*_ready
        rm -f {{ workdir }}/state/txgen_start
      args:
        chdir: "{{ workdir }}"
      changed_when: true

    - name: Validate snapshot state files when skipping funding
      ansible.builtin.stat:
        path: "{{ item }}"
      loop:
        - "{{ workdir }}/state/funding_snapshot.json"
        - "{{ workdir }}/state/mining_targets.json"
      register: snapshot_state_files
      when: skip_funding_setup

    - name: Fail if snapshot state files are missing
      ansible.builtin.fail:
        msg: "Missing required snapshot state file: {{ item.stat.path }}"
      loop: "{{ snapshot_state_files.results | default([]) }}"
      when:
        - skip_funding_setup
        - not item.stat.exists

    - name: Build base argv for funding setup
      ansible.builtin.set_fact:
        funding_setup_argv:
          - python3
          - "{{ workdir }}/files/funding_setup.py"
          - "--rpc-user"
          - "{{ rpc_user }}"
          - "--rpc-pass"
          - "{{ rpc_pass }}"
          - "--funding-host"
          - "{{ funding_host_port }}"
          - "--funding-wallet"
          - "{{ wallet_funding_name }}"
          - "--node-count"
          - "{{ node_count | string }}"
          - "--mining-percentage"
          - "{{ mining_percentage | string }}"
          - "--utxo-per-shard"
          - "{{ wallet_descriptor_pool_size | string }}"
          - "--utxo-amount"
          - "{{ '%0.8f' | format(wallet_descriptor_amount_btc) }}"
          - "--address-batch"
          - "{{ funding_address_batch | default(50) | string }}"
          - "--confirmation-blocks"
          - "{{ funding_confirmation_blocks | string }}"
          - "--state-dir"
          - "{{ workdir }}/state"
          - "--scheduler-config"
          - "{{ workdir }}/state/mining_targets.json"
          - "--summary-file"
          - "{{ workdir }}/state/funding_snapshot.json"
          - "--scheduler-interval"
          - "{{ block_scheduler_interval_s | string }}"
          - "--scheduler-seed"
          - "{{ block_scheduler_seed | string }}"
          - "--miner-host-override"
          - "127.0.0.1"
          - "--miner-port-base"
          - "{{ node_rpc_base | string }}"
          - "--miner-port-step"
          - "{{ node_rpc_step | string }}"
      when: not skip_funding_setup

    - name: Append shard-specific arguments
      ansible.builtin.set_fact:
        funding_setup_argv: "{{ funding_setup_argv + ['--shard', shard_spec] }}"
      vars:
        shard_spec: "id={{ item.id }},host=127.0.0.1:{{ wallet_rpc_base + (wallet_rpc_step * (loop_index + 1)) }},wallet={{ item.wallet_name }}"
      loop: "{{ txgen_shards }}"
      loop_control:
        index_var: loop_index
      when: not skip_funding_setup

    - name: Execute funding setup
      ansible.builtin.command:
        argv: "{{ funding_setup_argv }}"
        chdir: "{{ workdir }}"
      environment:
        PYTHONUNBUFFERED: "1"
      register: funding_setup
      changed_when: true
      when: not skip_funding_setup

    - name: Debug funding setup output
      ansible.builtin.debug:
        msg: "{{ funding_setup.stdout_lines }}"
      when: not skip_funding_setup

    - name: Verify funding snapshot exists
      ansible.builtin.stat:
        path: "{{ workdir }}/state/funding_snapshot.json"
      register: funding_snapshot
      failed_when: not funding_snapshot.stat.exists

    - name: Start TxGen containers after successful funding setup
      ansible.builtin.shell: |
        docker compose -f {{ workdir }}/docker-compose.yml --profile txgen up -d txgen_{{ item.id }}
      args:
        chdir: "{{ workdir }}"
      loop: "{{ txgen_shards }}"
      changed_when: true
      register: txgen_start
      when: not skip_txgen_start | default(false) | bool

    - name: Wait for TxGen containers to be running
      ansible.builtin.shell: |
        timeout=30
        elapsed=0
        while [ $elapsed -lt $timeout ]; do
          if docker ps --filter "name=txgen_{{ item.id }}" --format "{{ '{{.Names}}' }}" | grep -q "^txgen_{{ item.id }}$"; then
            echo "Container txgen_{{ item.id }} is running"
            exit 0
          fi
          sleep 1
          elapsed=$((elapsed + 1))
        done
        echo "Timeout waiting for txgen_{{ item.id }}"
        exit 1
      loop: "{{ txgen_shards }}"
      register: txgen_status
      changed_when: false
      when: not skip_txgen_start | default(false) | bool
